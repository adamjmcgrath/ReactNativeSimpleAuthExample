import { AppRegistry } from 'react-native';
import ReactNativeSimpleAuthExample from './app';

AppRegistry.registerComponent(
  'ReactNativeSimpleAuthExample',
  () => ReactNativeSimpleAuthExample
);
